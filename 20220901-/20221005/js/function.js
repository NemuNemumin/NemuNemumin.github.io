// 関数作成(まだ実行されない)
function logHello(text){
    //``を使った文字列では、変数を埋め込める
  console.log(`関数だったりするかも${text}`);
}
function logabc(a,b,c){
  console.log(`${a}${b}${c}`);
}

function logDefault(d="デフォルト"){
  console.log(`${d}`);
}

